version: '3.8'
services:
  smartinum-mongodb:
    image: mongo:4.4.4
    environment:
      #   - MONGO_INITDB_DATABASE=smartinum
      - MONGO_INITDB_ROOT_USERNAME=sa
      - MONGO_INITDB_ROOT_PASSWORD=yourStrong(!)Password
    ports:
      - '27018:27017'
    # volumes:
    #     - ~/volumes/jhipster/smartinum/mongodb/:/data/db/
